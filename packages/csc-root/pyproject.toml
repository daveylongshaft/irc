[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "csc-root"
version = "0.1.0"
description = "Independent Root layer package for the CSC inheritance chain."
requires-python = ">=3.8"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
