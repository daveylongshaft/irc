[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "csc-bridge"
version = "0.1.0"
description = "IRC protocol bridge for CSC system."
requires-python = ">=3.8"
dependencies = [
    "csc-network>=0.1.0",
]

[project.scripts]
csc-bridge = "csc_bridge.main:main"

[tool.setuptools.packages.find]
where = ["."]
