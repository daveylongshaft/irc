[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "csc-crypto"
version = "0.1.0"
description = "Cryptographic layer for CSC network transports."
requires-python = ">=3.8"
dependencies = [
    "csc-network>=0.1.0",
]

[tool.setuptools.packages.find]
where = ["."]
