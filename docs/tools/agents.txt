AGENTS SUBMODULE MAP
==================================================

── bin/agent ──
# #!/usr/bin/env python3

── bin/agent.bat ──
# @echo off

── bin/manual-agent-runner.md ──
# # Manual Agent Runner

── bin/run_agent.bat ──
# @echo off

── bin/run_agent.py ──
# Universal agent runner — detects agent type from directory name and
  CLAUDE_AGENTS = {'haiku': 'claude-haiku-4-5-20251001', 'sonnet': 'claude-sonnet-4-5-20250929'...
  GEMINI_AGENTS = {'gemini-3-pro': 'gemini-2.5-pro', 'gemini-3-flash': 'gemini-2.5-flash', 'gem...
  LOCAL_AGENTS = {'ollama-codellama': 'codellama', 'ollama-deepseek': 'deepseek-coder', 'ollam...
  def detect_agent_name(workorder_path: Path) -> str  # Derive agent name from directory structure.
  def find_csc_root(workorder_path: Path) -> Path  # Walk up from workorder to find project root (has CLAUDE.md).
  def _make_env(**extras)  # Create a clean subprocess environment with UTF-8 encoding.
  def run_claude(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Claude Code CLI.
  def run_gemini(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Gemini CLI via npx.
  def run_local(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke local model via ollama CLI.
  def main()

── bin/track_agents.sh ──
# #!/bin/bash

── irc/packages/csc-service/csc_service/shared/services/agent_service.py ──
  class agent(Service):  # AI agent runner service.
    def PROMPTS_BASE(self)
    def READY_DIR(self)
    def WIP_DIR(self)
    def DONE_DIR(self)
    def _build_cmd(self, agent_name, prompt, wip_filename, repo_clone_path=None)  # Build the command for the given agent.
    def __init__(self, server_instance)
    def _is_running(self)  # Check if the tracked agent process is still alive.
    def _find_prompt(self, filename)  # Find a prompt file in ready/ or wip/. Returns Path or None.
    def _read_text_safe(path)  # Read a text file, trying utf-8 first then falling back to latin-1.
    def _create_queue_metadata(self, agent_name: str, workorder_name: str, original_path: Path) -> dict  # Create metadata dictionary for a queued workorder.
    def _ensure_agent_dirs(self, agent_dir, agent_name)  # Create agent directory structure and copy default template if missing.
    def _detect_agents(self)  # Return dict of agent name -> available (bool).
    def _parse_front_matter(filepath)  # Parse YAML-like front-matter from a prompt file.
    def _check_prompt_capabilities(self, filepath)  # Check if this system can satisfy a prompt's requirements.
    def _build_prompt(self, wip_path)  # Assemble the full prompt from README.1shot + agent context dir + WIP content.
    def list(self) -> str  # List available AI agent backends.
    def select(self, name: str) -> str  # Select which AI agent backend to use.
    def assign(self, prompt_filename: str) -> str  # Assign a prompt to the selected agent for queue-based processing.
    def _write_queued_files(self, wip_path: Path, metadata: dict, agent_name: str, workorder_filename: str)  # Writes the workorder content and its metadata to the agent's queue/in/ directory.
    def _run_generate_orders_md_script(self, agent_name: str, workorder_filename: str)  # Run the generate_orders_md script to create orders.md in agent's queue/in/.
    def _format_elapsed(self, started)  # Format elapsed time from a start timestamp.
    def _get_proc_mem(self, pid)  # Get resident memory of a process in MB from /proc. Returns str or '?'.
    def _get_log_size(self, log_path)  # Get log file size as human-readable string.
    def _get_wip_progress(self, prompt_name)  # Count completed/next/pending steps in WIP file.
    def status(self) -> str  # Show queue and agent status by scanning queue directories and WIP files.
    def _is_running_pid(self, pid)  # Check if a specific PID is alive.
    def stop(self) -> str  # Stop the running agent.
    def kill(self) -> str  # Kill the running agent and move WIP prompt back to ready.
    def tail(self, *args) -> str  # Tail the WIP journal file to see agent progress.
    def _find_temp_repo_wips(self)  # Find WIP files in agent temp repos (where running agents write).
    def _find_wip_file(self, filename)  # Find a workorder file in wip/, done/, or ready/. Returns Path or None.
    def _tail_file(self, wip_path, n=20, source=None)  # Return the last N lines of a file with a header.
    def _check_stale(self)  # Check if the WIP file hasn't been updated recently. Returns warning or None.
    def _clear_state(self)  # Reset agent tracking state.
    def _git_sync(self, msg)  # Commit staged changes, push, pull.
    def default(self, *args) -> str  # Show available commands.

── irc/packages/csc-service/csc_service/shared/services/run_agent_executor.py ──
# RunAgentExecutor: Pure Python executor for running AI agents without bash/batch scripts.
  class RunAgentExecutor:  # Execute an AI agent for a queued workorder.
    def __init__(self, agent_name: str, queue_entry_path: Path, project_root: Optional[Path]=None)  # Initialize executor with queue entry metadata.
    def _find_project_root() -> Path  # Find project root by looking for CLAUDE.md or csc-service.json.
    def _load_queue_entry(self) -> bool  # Load queue metadata from orders.json in same directory as orders.md.
    def _prepare_environment(self) -> Dict[str, str]  # Prepare environment variables for agent execution.
    def _build_command(self) -> Optional[Tuple[list, str]]  # Build the claude command for executing the agent.
    def _journal_start(self) -> bool  # Write START marker to WIP file with timestamp.
    def _journal_complete(self) -> bool  # Write COMPLETE marker to WIP file.
    def _journal_error(self, message: str)  # Write error message to WIP file (and stderr).
    def execute(self) -> int  # Main execution flow: load, prepare, build, run, journal.
  def main()  # Entry point when run as: python run_agent_executor.py <agent> <queue_entry>

── ops/agents/chatgpt/bin/run_agent.bat ──
# @echo off

── ops/agents/chatgpt/bin/run_agent.py ──
# Universal agent runner — detects agent type from directory name and
  CLAUDE_AGENTS = {'haiku': 'claude-haiku-4-5-20251001', 'sonnet': 'claude-sonnet-4-5-20250929'...
  GEMINI_AGENTS = {'gemini-3-pro': 'gemini-2.5-pro', 'gemini-3-flash': 'gemini-2.5-flash', 'gem...
  LOCAL_AGENTS = {'ollama-codellama': 'codellama', 'ollama-deepseek': 'deepseek-coder', 'ollam...
  def detect_agent_name(workorder_path: Path) -> str  # Derive agent name from directory structure or environment.
  def find_csc_root(workorder_path: Path) -> Path  # Walk up from workorder to find project root (has CLAUDE.md).
  def _make_env(**extras)  # Create a clean subprocess environment with UTF-8 encoding.
  def run_claude(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Claude Code CLI.
  def run_gemini(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Gemini CLI via npx.
  def run_local(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke local model via ollama CLI.
  def main()

── ops/agents/chatgpt/cagent.yaml ──
# version: "2"

── ops/agents/claude-batch/cagent.yaml ──
# version: "2"

── ops/agents/claude-batch/orders.md-template ──
# You are an AI coding agent running from /opt/.

── ops/agents/claude/bin/run_agent.bat ──
# @echo off

── ops/agents/claude/bin/run_agent.py ──
# Universal agent runner — detects agent type from directory name and
  ANTHROPIC_AGENTS = {'haiku': 'claude-haiku-4-5-20251001', 'sonnet': 'claude-sonnet-4-5-20250929'...
  CLAUDE_AGENTS = ANTHROPIC_AGENTS
  GEMINI_AGENTS = {'gemini-3-pro': 'gemini-2.5-pro', 'gemini-3-flash': 'gemini-2.5-flash', 'gem...
  LOCAL_AGENTS = {'ollama-codellama': 'codellama', 'ollama-deepseek': 'deepseek-coder', 'ollam...
  MAX_TURNS = 100
  OUTPUT_CAP = 50000
  MAX_TOKENS_BY_MODEL = {'claude-opus-4-6': 32768, 'claude-sonnet-4-5-20250929': 32768, 'claude-haiku...
  DEFAULT_MAX_TOKENS = 16384
  SKIP_DIRS = {'.git', 'node_modules', '.trash', '__pycache__', '.venv', 'venv', '.tox'}
  CLAUDE_SYSTEM_PROMPT = 'You are an AI coding agent working on the CSC project. Your working director...
  def detect_agent_name(workorder_path: Path) -> str  # Derive agent name from directory structure or environment.
  def find_csc_root(workorder_path: Path) -> Path  # Walk up from workorder to find project root (has CLAUDE.md).
  def _make_env(**extras)  # Create a clean subprocess environment with UTF-8 encoding.
  def build_tools()  # Define the 6 tools available to Anthropic agents, matching Claude Code.
  def _safe_path(csc_root: Path, relative: str) -> Path  # Resolve a relative path within csc_root, preventing directory traversal.
  def execute_tool(name: str, tool_input: dict, csc_root: Path) -> str  # Execute a tool call locally, return result string.
  def build_cached_system_prompt(csc_root: Path)  # Build system prompt from project context files with cache_control.
  def log_usage(turn: int, usage)  # Log token usage for a single turn, including cache stats.
  def run_anthropic_api(agent_name: str, model: str, user_prompt: str, csc_root: Path)  # Run workorder via Anthropic Messages API with tools + prompt caching.
  def run_claude_cli(agent_name: str, model: str, user_prompt: str, csc_root: Path)  # Invoke Claude Code CLI with system prompt (legacy fallback).
  def run_gemini(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Gemini CLI (installed locally, not via npx).
  def run_local(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke local model via ollama CLI.
  def main()

── ops/agents/claude/cagent.yaml ──
# version: "2"

── ops/agents/claude/context/project-map.md ──
# # CSC Project Map - Quick Reference

── ops/agents/claude/context/test-guidelines.md ──
# # Test Writing Guidelines

── ops/agents/codellama/bin/run_agent.bat ──
# @echo off

── ops/agents/codellama/bin/run_agent.py ──
# Universal agent runner — detects agent type from directory name and
  CLAUDE_AGENTS = {'haiku': 'claude-haiku-4-5-20251001', 'sonnet': 'claude-sonnet-4-5-20250929'...
  GEMINI_AGENTS = {'gemini-3-pro': 'gemini-2.5-pro', 'gemini-3-flash': 'gemini-2.5-flash', 'gem...
  LOCAL_AGENTS = {'ollama-codellama': 'codellama', 'ollama-deepseek': 'deepseek-coder', 'ollam...
  def detect_agent_name(workorder_path: Path) -> str  # Derive agent name from directory structure or environment.
  def find_csc_root(workorder_path: Path) -> Path  # Walk up from workorder to find project root (has CLAUDE.md).
  def _make_env(**extras)  # Create a clean subprocess environment with UTF-8 encoding.
  def run_claude(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Claude Code CLI.
  def run_gemini(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Gemini CLI via npx.
  def run_local(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke local model via ollama CLI.
  def main()

── ops/agents/codellama/bin/run_agent.sh ──
# #!/bin/bash

── ops/agents/codellama/cagent.yaml ──
# version: "2"

── ops/agents/codellama/context/project-map.md ──
# # CSC Project Map - Quick Reference

── ops/agents/codellama/context/test-guidelines.md ──
# # Test Writing Guidelines

── ops/agents/deepseek/bin/run_agent.bat ──
# @echo off

── ops/agents/deepseek/bin/run_agent.py ──
# Universal agent runner — detects agent type from directory name and
  CLAUDE_AGENTS = {'haiku': 'claude-haiku-4-5-20251001', 'sonnet': 'claude-sonnet-4-5-20250929'...
  GEMINI_AGENTS = {'gemini-3-pro': 'gemini-2.5-pro', 'gemini-3-flash': 'gemini-2.5-flash', 'gem...
  LOCAL_AGENTS = {'ollama-codellama': 'codellama', 'ollama-deepseek': 'deepseek-coder', 'ollam...
  def detect_agent_name(workorder_path: Path) -> str  # Derive agent name from directory structure or environment.
  def find_csc_root(workorder_path: Path) -> Path  # Walk up from workorder to find project root (has CLAUDE.md).
  def _make_env(**extras)  # Create a clean subprocess environment with UTF-8 encoding.
  def run_claude(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Claude Code CLI.
  def run_gemini(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Gemini CLI via npx.
  def run_local(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke local model via ollama CLI.
  def main()

── ops/agents/deepseek/bin/run_agent.sh ──
# #!/bin/bash

── ops/agents/deepseek/cagent.yaml ──
# version: "2"

── ops/agents/deepseek/context/project-map.md ──
# # CSC Project Map - Quick Reference

── ops/agents/deepseek/context/test-guidelines.md ──
# # Test Writing Guidelines

── ops/agents/dmr_qwen_task_runner/bin/run_agent.bat ──
# @echo off

── ops/agents/dmr_qwen_task_runner/bin/run_agent.py ──
# Universal agent runner — detects agent type from directory name and
  CLAUDE_AGENTS = {'haiku': 'claude-haiku-4-5-20251001', 'sonnet': 'claude-sonnet-4-5-20250929'...
  GEMINI_AGENTS = {'gemini-3-pro': 'gemini-2.5-pro', 'gemini-3-flash': 'gemini-2.5-flash', 'gem...
  LOCAL_AGENTS = {'ollama-codellama': 'codellama', 'ollama-deepseek': 'deepseek-coder', 'ollam...
  def detect_agent_name(workorder_path: Path) -> str  # Derive agent name from directory structure or environment.
  def find_csc_root(workorder_path: Path) -> Path  # Walk up from workorder to find project root (has CLAUDE.md).
  def _make_env(**extras)  # Create a clean subprocess environment with UTF-8 encoding.
  def run_claude(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Claude Code CLI.
  def run_gemini(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Gemini CLI via npx.
  def run_local(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke local model via ollama CLI.
  def main()

── ops/agents/dmr_qwen_task_runner/bin/run_agent.sh ──
# #!/bin/bash

── ops/agents/dmr_qwen_task_runner/cagent.yaml ──
# # cagent.yaml for DMR Qwen task runner

── ops/agents/gemini-2.5-flash-lite/bin/run_agent.bat ──
# @echo off

── ops/agents/gemini-2.5-flash-lite/bin/run_agent.py ──
# Universal agent runner — detects agent type from directory name and
  CLAUDE_AGENTS = {'haiku': 'claude-haiku-4-5-20251001', 'sonnet': 'claude-sonnet-4-5-20250929'...
  GEMINI_AGENTS = {'gemini-3-pro': 'gemini-2.5-pro', 'gemini-3-flash': 'gemini-2.5-flash', 'gem...
  LOCAL_AGENTS = {'ollama-codellama': 'codellama', 'ollama-deepseek': 'deepseek-coder', 'ollam...
  def detect_agent_name(workorder_path: Path) -> str  # Derive agent name from directory structure or environment.
  def find_csc_root(workorder_path: Path) -> Path  # Walk up from workorder to find project root (has CLAUDE.md).
  def _make_env(**extras)  # Create a clean subprocess environment with UTF-8 encoding.
  def run_claude(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Claude Code CLI.
  def run_gemini(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Gemini CLI (installed locally, not via npx).
  def run_local(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke local model via ollama CLI.
  def main()

── ops/agents/gemini-2.5-flash-lite/cagent.yaml ──
# version: "2"

── ops/agents/gemini-2.5-flash-lite/context/agent-types.md ──
# # Generic Agent Types

── ops/agents/gemini-2.5-flash-lite/context/project-map.md ──
# # CSC Project Map - Quick Reference

── ops/agents/gemini-2.5-flash-lite/context/system.md ──
# # Generic Agent - Simple Task Execution

── ops/agents/gemini-2.5-flash-lite/context/test-guidelines.md ──
# # Test Writing Guidelines

── ops/agents/gemini-2.5-flash-lite/state.json ──
# {

── ops/agents/gemini-2.5-flash/bin/run_agent.bat ──
# @echo off

── ops/agents/gemini-2.5-flash/bin/run_agent.py ──
# Universal agent runner — detects agent type from directory name and
  CLAUDE_AGENTS = {'haiku': 'claude-haiku-4-5-20251001', 'sonnet': 'claude-sonnet-4-5-20250929'...
  GEMINI_AGENTS = {'gemini-3-pro': 'gemini-2.5-pro', 'gemini-3-flash': 'gemini-2.5-flash', 'gem...
  LOCAL_AGENTS = {'ollama-codellama': 'codellama', 'ollama-deepseek': 'deepseek-coder', 'ollam...
  def detect_agent_name(workorder_path: Path) -> str  # Derive agent name from directory structure or environment.
  def find_csc_root(workorder_path: Path) -> Path  # Walk up from workorder to find project root (has CLAUDE.md).
  def _make_env(**extras)  # Create a clean subprocess environment with UTF-8 encoding.
  def run_claude(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Claude Code CLI.
  def run_gemini(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Gemini CLI (installed locally, not via npx).
  def run_local(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke local model via ollama CLI.
  def main()

── ops/agents/gemini-2.5-flash/bin/run_agent.sh ──
# #!/bin/bash

── ops/agents/gemini-2.5-flash/cagent.yaml ──
# version: "2"

── ops/agents/gemini-2.5-flash/context/debugging.md ──
# # Debugging Process: Qwen Integration

── ops/agents/gemini-2.5-flash/context/project-map.md ──
# # CSC Project Map - Quick Reference

── ops/agents/gemini-2.5-flash/context/system.md ──
# # System Context: Qwen Integration Fix

── ops/agents/gemini-2.5-flash/context/test-guidelines.md ──
# # Test Writing Guidelines

── ops/agents/gemini-2.5-flash/context/validation.md ──
# # Validation Checklist: Qwen Integration

── ops/agents/gemini-2.5-flash/orders.md-template ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-2.5-flash/queue/out/orders-1772627097.md ──
# You are an AI coding agent. Your working directory is the CSC project root.

── ops/agents/gemini-2.5-pro/bin/run_agent.bat ──
# @echo off

── ops/agents/gemini-2.5-pro/bin/run_agent.py ──
# Universal agent runner — detects agent type from directory name and
  CLAUDE_AGENTS = {'haiku': 'claude-haiku-4-5-20251001', 'sonnet': 'claude-sonnet-4-5-20250929'...
  GEMINI_AGENTS = {'gemini-3-pro': 'gemini-2.5-pro', 'gemini-3-flash': 'gemini-2.5-flash', 'gem...
  LOCAL_AGENTS = {'ollama-codellama': 'codellama', 'ollama-deepseek': 'deepseek-coder', 'ollam...
  def detect_agent_name(workorder_path: Path) -> str  # Derive agent name from directory structure or environment.
  def find_csc_root(workorder_path: Path) -> Path  # Walk up from workorder to find project root (has CLAUDE.md).
  def _make_env(**extras)  # Create a clean subprocess environment with UTF-8 encoding.
  def run_claude(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Claude Code CLI.
  def run_gemini(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Gemini CLI (installed locally, not via npx).
  def run_local(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke local model via ollama CLI.
  def main()

── ops/agents/gemini-2.5-pro/bin/run_agent.sh ──
# #!/bin/bash

── ops/agents/gemini-2.5-pro/orders.md-template ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-2.5-pro/queue/out/agent_1773105745_PROMPT_review_pr_daveylongshaft_irc_1_20260309-201234.log ──
# Updating 083aef9..b890419

── ops/agents/gemini-2.5-pro/queue/out/agent_1773106341_PROMPT_review_pr_daveylongshaft_irc_1_20260309-201234.log ──
# Invoking: gemini -y -m gemini-2.5-pro -p " "

── ops/agents/gemini-2.5-pro/queue/out/agent_1773106565_haiku-fix_agent_tail_segfault.log ──
# Invoking: gemini -y -m gemini-2.5-pro -p " "

── ops/agents/gemini-2.5-pro/queue/out/agent_1773108135_csc-ftpd-wo-watcher.log ──
# Invoking: /home/davey/.local/bin/claude --dangerously-skip-permissions --model opus -p -

── ops/agents/gemini-2.5-pro/queue/out/agent_1773109225_IMPLEMENT_JULES_MONITORING_AND_APPROVAL_SERVICE.log ──
# Already up to date.

── ops/agents/gemini-2.5-pro/queue/out/agent_1773111034_JULES_INTEGRATION_WORKORDER_SPEC.log ──
# Already up to date.

── ops/agents/gemini-2.5-pro/queue/out/agent_1773111393_PLAN_REVIEW_AGENT_SETUP.log ──
# Already up to date.

── ops/agents/gemini-2.5-pro/queue/out/agent_1773112108_REBUILD_AGENT_AND_WORKORDERS_SERVICES.log ──
# Already up to date.

── ops/agents/gemini-2.5-pro/queue/out/agent_1773112754_create_file_comparison_script.log ──
# Already up to date.

── ops/agents/gemini-2.5-pro/queue/out/agent_1773113892_PROMPT_review_pr_daveylongshaft_irc_1_20260309-175457.log ──
# Already up to date.

── ops/agents/gemini-2.5-pro/queue/out/agent_1773183965_claude-evolution-story.log ──
# Already up to date.

── ops/agents/gemini-2.5-pro/queue/out/agent_1773186212_PROMPT_review_pr_daveylongshaft_irc_2_20260310-183830.log ──
# Already up to date.

── ops/agents/gemini-2.5-pro/queue/out/agent_1773186650_PROMPT_review_pr_daveylongshaft_irc_2_20260310-184341.log ──
# Already up to date.

── ops/agents/gemini-2.5-pro/queue/out/agent_1773186948_PROMPT_review_pr_daveylongshaft_irc_2_20260310-185059.log ──
# Already up to date.

── ops/agents/gemini-2.5-pro/queue/out/agent_1773188191_PROMPT_review_pr_daveylongshaft_irc_2_20260310-185557.log ──
# Already up to date.

── ops/agents/gemini-2.5-pro/queue/out/agent_1773192148_PROMPT_review_pr_daveylongshaft_irc_3_20260310-201343.log ──
# Already up to date.

── ops/agents/gemini-2.5-pro/queue/out/agent_1773192304_PROMPT_review_pr_daveylongshaft_irc_3_20260310-201345.log ──
# Already up to date.

── ops/agents/gemini-2.5-pro/queue/out/agent_1773192536_PROMPT_review_pr_daveylongshaft_irc_3_20260310-202514.log ──
# Already up to date.

── ops/agents/gemini-2.5-pro/queue/out/agent_1773192698_PROMPT_review_pr_daveylongshaft_irc_3_20260310-202906.log ──
# Already up to date.

── ops/agents/gemini-2.5-pro/queue/out/agent_1773206523_oper-hierarchy-20260311.log ──
# Already up to date.

── ops/agents/gemini-2.5-pro/queue/out/agent_1773216377_PKI_CERTIFICATE_ENROLLMENT_SYSTEM.log ──
# Already up to date.

── ops/agents/gemini-2.5-pro/queue/out/agent_1773228445_PROMPT_setup_github_webhook.log ──
# Already up to date.

── ops/agents/gemini-2.5-pro/queue/out/agent_1773229109_windows-service-update.log ──
# Already up to date.

── ops/agents/gemini-2.5-pro/queue/out/agent_1773229264_windows-service-update.log ──
# Already up to date.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773106043.md ──
# You are an AI coding agent. Your working directory is the CSC project root.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773106533.md ──
# You are an AI coding agent. Your working directory is the CSC project root.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773106820.md ──
# You are an AI coding agent. Your working directory is the CSC project root.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773109448.md ──
# You are an AI coding agent. Your working directory is a clone of the CSC IRC code repo.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773109661.md ──
# You are an AI coding agent. Your working directory is a clone of the CSC IRC code repo.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773111321.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773111468.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773111614.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773112181.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773112328.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773112830.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773113986.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773184107.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773186642.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773186939.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773187463.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773188331.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773192296.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773192528.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773192617.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773192773.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773192930.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773206598.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773216451.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773217484.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773217651.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773223303.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773223465.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773228518.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773228673.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773229180.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-2.5-pro/queue/out/orders-1773229336.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-2.5-pro/queue/out/orders-orphan-1773105219.md ──
# You are an AI coding agent. Your working directory is the CSC project root.

── ops/agents/gemini-2.5-pro/queue/out/orders-orphan-1773106191.md ──
# You are an AI coding agent. Your working directory is the CSC project root.

── ops/agents/gemini-3-flash-preview/bin/run_agent.bat ──
# @echo off

── ops/agents/gemini-3-flash-preview/bin/run_agent.py ──
# Universal agent runner — detects agent type from directory name and
  CLAUDE_AGENTS = {'haiku': 'claude-haiku-4-5-20251001', 'sonnet': 'claude-sonnet-4-5-20250929'...
  GEMINI_AGENTS = {'gemini-3-pro': 'gemini-2.5-pro', 'gemini-3-flash': 'gemini-2.5-flash', 'gem...
  LOCAL_AGENTS = {'ollama-codellama': 'codellama', 'ollama-deepseek': 'deepseek-coder', 'ollam...
  def detect_agent_name(workorder_path: Path) -> str  # Derive agent name from directory structure or environment.
  def find_csc_root(workorder_path: Path) -> Path  # Walk up from workorder to find project root (has CLAUDE.md).
  def _make_env(**extras)  # Create a clean subprocess environment with UTF-8 encoding.
  def run_claude(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Claude Code CLI.
  def run_gemini(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Gemini CLI (installed locally, not via npx).
  def run_local(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke local model via ollama CLI.
  def main()

── ops/agents/gemini-3-flash-preview/cagent.yaml ──
# version: "2"

── ops/agents/gemini-3-flash/bin/run_agent.bat ──
# @echo off

── ops/agents/gemini-3-flash/bin/run_agent.py ──
# Universal agent runner — detects agent type from directory name and
  CLAUDE_AGENTS = {'haiku': 'claude-haiku-4-5-20251001', 'sonnet': 'claude-sonnet-4-5-20250929'...
  GEMINI_AGENTS = {'gemini-3-pro': 'gemini-2.5-pro', 'gemini-3-flash': 'gemini-2.5-flash', 'gem...
  LOCAL_AGENTS = {'ollama-codellama': 'codellama', 'ollama-deepseek': 'deepseek-coder', 'ollam...
  def detect_agent_name(workorder_path: Path) -> str  # Derive agent name from directory structure or environment.
  def find_csc_root(workorder_path: Path) -> Path  # Walk up from workorder to find project root (has CLAUDE.md).
  def _make_env(**extras)  # Create a clean subprocess environment with UTF-8 encoding.
  def run_claude(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Claude Code CLI.
  def run_gemini(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Gemini CLI (installed locally, not via npx).
  def run_local(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke local model via ollama CLI.
  def main()

── ops/agents/gemini-3-flash/bin/run_agent.sh ──
# #!/bin/bash

── ops/agents/gemini-3-flash/cagent.yaml ──
# version: "2"

── ops/agents/gemini-3-flash/context/project-map.md ──
# # CSC Project Map - Quick Reference

── ops/agents/gemini-3-flash/context/test-guidelines.md ──
# # Test Writing Guidelines

── ops/agents/gemini-3-pro-preview/bin/run_agent.bat ──
# @echo off

── ops/agents/gemini-3-pro-preview/bin/run_agent.py ──
# Universal agent runner — detects agent type from directory name and
  CLAUDE_AGENTS = {'haiku': 'claude-haiku-4-5-20251001', 'sonnet': 'claude-sonnet-4-5-20250929'...
  GEMINI_AGENTS = {'gemini-3-pro': 'gemini-2.5-pro', 'gemini-3-flash': 'gemini-2.5-flash', 'gem...
  LOCAL_AGENTS = {'ollama-codellama': 'codellama', 'ollama-deepseek': 'deepseek-coder', 'ollam...
  def detect_agent_name(workorder_path: Path) -> str  # Derive agent name from directory structure or environment.
  def find_csc_root(workorder_path: Path) -> Path  # Walk up from workorder to find project root (has CLAUDE.md).
  def _make_env(**extras)  # Create a clean subprocess environment with UTF-8 encoding.
  def run_claude(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Claude Code CLI.
  def run_gemini(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Gemini CLI (installed locally, not via npx).
  def run_local(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke local model via ollama CLI.
  def main()

── ops/agents/gemini-3-pro-preview/cagent.yaml ──
# version: "2"

── ops/agents/gemini-3-pro/bin/run_agent.bat ──
# @echo off

── ops/agents/gemini-3-pro/bin/run_agent.py ──
# Universal agent runner — detects agent type from directory name and
  CLAUDE_AGENTS = {'haiku': 'claude-haiku-4-5-20251001', 'sonnet': 'claude-sonnet-4-5-20250929'...
  GEMINI_AGENTS = {'gemini-3-pro': 'gemini-2.5-pro', 'gemini-3-flash': 'gemini-2.5-flash', 'gem...
  LOCAL_AGENTS = {'ollama-codellama': 'codellama', 'ollama-deepseek': 'deepseek-coder', 'ollam...
  def detect_agent_name(workorder_path: Path) -> str  # Derive agent name from directory structure or environment.
  def find_csc_root(workorder_path: Path) -> Path  # Walk up from workorder to find project root (has CLAUDE.md).
  def _make_env(**extras)  # Create a clean subprocess environment with UTF-8 encoding.
  def run_claude(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Claude Code CLI.
  def run_gemini(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Gemini CLI (installed locally, not via npx).
  def run_local(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke local model via ollama CLI.
  def main()

── ops/agents/gemini-3-pro/bin/run_agent.sh ──
# #!/bin/bash

── ops/agents/gemini-3-pro/cagent.yaml ──
# version: "2"

── ops/agents/gemini-3-pro/context/project-map.md ──
# # CSC Project Map - Quick Reference

── ops/agents/gemini-3-pro/context/test-guidelines.md ──
# # Test Writing Guidelines

── ops/agents/gemini-3-pro/orders.md-template ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-3-pro/queue/out/orders-1772625903.md ──
# You are an AI coding agent. Your working directory is the CSC project root.

── ops/agents/gemini-3-pro/queue/out/orders-1772626272.md ──
# You are an AI coding agent. Your working directory is the CSC project root.

── ops/agents/gemini-3.1-pro-preview/bin/run_agent.bat ──
# @echo off

── ops/agents/gemini-3.1-pro-preview/bin/run_agent.py ──
# Universal agent runner — detects agent type from directory name and
  CLAUDE_AGENTS = {'haiku': 'claude-haiku-4-5-20251001', 'sonnet': 'claude-sonnet-4-5-20250929'...
  GEMINI_AGENTS = {'gemini-3.1-pro-preview': 'gemini-3.1-pro-preview', 'gemini-3.1-flash-previe...
  LOCAL_AGENTS = {'ollama-codellama': 'codellama', 'ollama-deepseek': 'deepseek-coder', 'ollam...
  def detect_agent_name(workorder_path: Path) -> str  # Derive agent name from directory structure or environment.
  def find_csc_root(workorder_path: Path) -> Path  # Walk up from workorder to find project root (has .csc_root).
  def _make_env(**extras)  # Create a clean subprocess environment with UTF-8 encoding.
  def run_claude(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Claude Code CLI.
  def run_gemini(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Gemini CLI (installed locally, not via npx).
  def run_local(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke local model via ollama CLI.
  def main()

── ops/agents/gemini-3.1-pro-preview/bin/run_agent.sh ──
# #!/bin/bash

── ops/agents/gemini-3.1-pro-preview/cagent.yaml ──
# version: "2"

── ops/agents/gemini-3.1-pro-preview/context/project-map.md ──
# # CSC Project Map - Quick Reference

── ops/agents/gemini-3.1-pro-preview/context/test-guidelines.md ──
# # Test Writing Guidelines

── ops/agents/gemini-3.1-pro-preview/orders.md-template ──
# You are an AI coding agent running from /opt/.

── ops/agents/gemini-3.1-pro-preview/queue/out/orders-1772625903.md ──
# You are an AI coding agent. Your working directory is the CSC project root.

── ops/agents/gemini-3.1-pro-preview/queue/out/orders-1772626272.md ──
# You are an AI coding agent. Your working directory is the CSC project root.

── ops/agents/gemini/bin/run_agent.bat ──
# @echo off

── ops/agents/gemini/bin/run_agent.py ──
# Universal agent runner — detects agent type from directory name and
  CLAUDE_AGENTS = {'haiku': 'claude-haiku-4-5-20251001', 'sonnet': 'claude-sonnet-4-5-20250929'...
  GEMINI_AGENTS = {'gemini-3-pro': 'gemini-2.5-pro', 'gemini-3-flash': 'gemini-2.5-flash', 'gem...
  LOCAL_AGENTS = {'ollama-codellama': 'codellama', 'ollama-deepseek': 'deepseek-coder', 'ollam...
  def detect_agent_name(workorder_path: Path) -> str  # Derive agent name from directory structure or environment.
  def find_csc_root(workorder_path: Path) -> Path  # Walk up from workorder to find project root (has CLAUDE.md).
  def _make_env(**extras)  # Create a clean subprocess environment with UTF-8 encoding.
  def run_claude(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Claude Code CLI.
  def run_gemini(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Gemini CLI (installed locally, not via npx).
  def run_local(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke local model via ollama CLI.
  def main()

── ops/agents/gemini/cagent.yaml ──
# version: "2"

── ops/agents/gemini/context/project-map.md ──
# # CSC Project Map - Quick Reference

── ops/agents/gemini/context/test-guidelines.md ──
# # Test Writing Guidelines

── ops/agents/haiku/bin/run_agent.bat ──
# @echo off

── ops/agents/haiku/bin/run_agent.py ──
# Universal agent runner — detects agent type from directory name and
  ANTHROPIC_AGENTS = {'haiku': 'claude-haiku-4-5-20251001', 'sonnet': 'claude-sonnet-4-5-20250929'...
  CLAUDE_AGENTS = ANTHROPIC_AGENTS
  GEMINI_AGENTS = {'gemini-3-pro': 'gemini-2.5-pro', 'gemini-3-flash': 'gemini-2.5-flash', 'gem...
  LOCAL_AGENTS = {'ollama-codellama': 'codellama', 'ollama-deepseek': 'deepseek-coder', 'ollam...
  MAX_TURNS = 100
  OUTPUT_CAP = 50000
  MAX_TOKENS_BY_MODEL = {'claude-opus-4-6': 32768, 'claude-sonnet-4-5-20250929': 32768, 'claude-haiku...
  DEFAULT_MAX_TOKENS = 16384
  SKIP_DIRS = {'.git', 'node_modules', '.trash', '__pycache__', '.venv', 'venv', '.tox'}
  CLAUDE_SYSTEM_PROMPT = 'You are an AI coding agent working on the CSC project. Your working director...
  def detect_agent_name(workorder_path: Path) -> str  # Derive agent name from directory structure or environment.
  def find_csc_root(workorder_path: Path) -> Path  # Walk up from workorder to find project root (has CLAUDE.md).
  def _make_env(**extras)  # Create a clean subprocess environment with UTF-8 encoding.
  def build_tools()  # Define the 6 tools available to Anthropic agents, matching Claude Code.
  def _safe_path(csc_root: Path, relative: str) -> Path  # Resolve a relative path within csc_root, preventing directory traversal.
  def execute_tool(name: str, tool_input: dict, csc_root: Path) -> str  # Execute a tool call locally, return result string.
  def build_cached_system_prompt(csc_root: Path)  # Build system prompt from project context files with cache_control.
  def log_usage(turn: int, usage)  # Log token usage for a single turn, including cache stats.
  def run_anthropic_api(agent_name: str, model: str, user_prompt: str, csc_root: Path)  # Run workorder via Anthropic Messages API with tools + prompt caching.
  def run_claude_cli(agent_name: str, model: str, user_prompt: str, csc_root: Path)  # Invoke Claude Code CLI with system prompt (legacy fallback).
  def run_gemini(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Gemini CLI (installed locally, not via npx).
  def run_local(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke local model via ollama CLI.
  def main()

── ops/agents/haiku/bin/run_agent.sh ──
# #!/bin/bash

── ops/agents/haiku/cagent.yaml ──
# version: "2"

── ops/agents/haiku/context/project-map.md ──
# # CSC Project Map - Quick Reference

── ops/agents/haiku/context/test-guidelines.md ──
# # Test Writing Guidelines

── ops/agents/haiku/orders.md-template ──
# You are an AI coding agent running from /opt/.

── ops/agents/ollama-codellama/bin/run_agent.bat ──
# @echo off

── ops/agents/ollama-codellama/bin/run_agent.py ──
# Universal agent runner — detects agent type from directory name and
  CLAUDE_AGENTS = {'haiku': 'claude-haiku-4-5-20251001', 'sonnet': 'claude-sonnet-4-5-20250929'...
  GEMINI_AGENTS = {'gemini-3-pro': 'gemini-2.5-pro', 'gemini-3-flash': 'gemini-2.5-flash', 'gem...
  LOCAL_AGENTS = {'ollama-codellama': 'codellama', 'ollama-deepseek': 'deepseek-coder', 'ollam...
  def detect_agent_name(workorder_path: Path) -> str  # Derive agent name from directory structure or environment.
  def find_csc_root(workorder_path: Path) -> Path  # Walk up from workorder to find project root (has CLAUDE.md).
  def _make_env(**extras)  # Create a clean subprocess environment with UTF-8 encoding.
  def run_claude(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Claude Code CLI.
  def run_gemini(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Gemini CLI via npx.
  def run_local(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke local model via ollama CLI.
  def main()

── ops/agents/ollama-codellama/bin/run_agent.sh ──
# #!/bin/bash

── ops/agents/ollama-deepseek/bin/run_agent.bat ──
# @echo off

── ops/agents/ollama-deepseek/bin/run_agent.py ──
# Universal agent runner — detects agent type from directory name and
  CLAUDE_AGENTS = {'haiku': 'claude-haiku-4-5-20251001', 'sonnet': 'claude-sonnet-4-5-20250929'...
  GEMINI_AGENTS = {'gemini-3-pro': 'gemini-2.5-pro', 'gemini-3-flash': 'gemini-2.5-flash', 'gem...
  LOCAL_AGENTS = {'ollama-codellama': 'codellama', 'ollama-deepseek': 'deepseek-coder', 'ollam...
  def detect_agent_name(workorder_path: Path) -> str  # Derive agent name from directory structure or environment.
  def find_csc_root(workorder_path: Path) -> Path  # Walk up from workorder to find project root (has CLAUDE.md).
  def _make_env(**extras)  # Create a clean subprocess environment with UTF-8 encoding.
  def run_claude(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Claude Code CLI.
  def run_gemini(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Gemini CLI via npx.
  def run_local(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke local model via ollama CLI.
  def main()

── ops/agents/ollama-deepseek/bin/run_agent.sh ──
# #!/bin/bash

── ops/agents/ollama-qwen/bin/run_agent.bat ──
# @echo off

── ops/agents/ollama-qwen/bin/run_agent.py ──
# Universal agent runner — detects agent type from directory name and
  CLAUDE_AGENTS = {'haiku': 'claude-haiku-4-5-20251001', 'sonnet': 'claude-sonnet-4-5-20250929'...
  GEMINI_AGENTS = {'gemini-3-pro': 'gemini-2.5-pro', 'gemini-3-flash': 'gemini-2.5-flash', 'gem...
  LOCAL_AGENTS = {'ollama-codellama': 'codellama', 'ollama-deepseek': 'deepseek-coder', 'ollam...
  def detect_agent_name(workorder_path: Path) -> str  # Derive agent name from directory structure or environment.
  def find_csc_root(workorder_path: Path) -> Path  # Walk up from workorder to find project root (has CLAUDE.md).
  def _make_env(**extras)  # Create a clean subprocess environment with UTF-8 encoding.
  def run_claude(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Claude Code CLI.
  def run_gemini(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Gemini CLI via npx.
  def run_local(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke local model via ollama CLI.
  def main()

── ops/agents/ollama-qwen/bin/run_agent.sh ──
# #!/bin/bash

── ops/agents/opus/bin/run_agent.bat ──
# @echo off

── ops/agents/opus/bin/run_agent.py ──
# Universal agent runner — detects agent type from directory name and
  ANTHROPIC_AGENTS = {'haiku': 'claude-haiku-4-5-20251001', 'sonnet': 'claude-sonnet-4-5-20250929'...
  CLAUDE_AGENTS = ANTHROPIC_AGENTS
  GEMINI_AGENTS = {'gemini-3-pro': 'gemini-2.5-pro', 'gemini-3-flash': 'gemini-2.5-flash', 'gem...
  LOCAL_AGENTS = {'ollama-codellama': 'codellama', 'ollama-deepseek': 'deepseek-coder', 'ollam...
  MAX_TURNS = 100
  OUTPUT_CAP = 50000
  MAX_TOKENS_BY_MODEL = {'claude-opus-4-6': 32768, 'claude-sonnet-4-5-20250929': 32768, 'claude-haiku...
  DEFAULT_MAX_TOKENS = 16384
  SKIP_DIRS = {'.git', 'node_modules', '.trash', '__pycache__', '.venv', 'venv', '.tox'}
  CLAUDE_SYSTEM_PROMPT = 'You are an AI coding agent working on the CSC project. Your working director...
  def detect_agent_name(workorder_path: Path) -> str  # Derive agent name from directory structure or environment.
  def find_csc_root(workorder_path: Path) -> Path  # Walk up from workorder to find project root (has CLAUDE.md).
  def _make_env(**extras)  # Create a clean subprocess environment with UTF-8 encoding.
  def build_tools()  # Define the 6 tools available to Anthropic agents, matching Claude Code.
  def _safe_path(csc_root: Path, relative: str) -> Path  # Resolve a relative path within csc_root, preventing directory traversal.
  def execute_tool(name: str, tool_input: dict, csc_root: Path) -> str  # Execute a tool call locally, return result string.
  def build_cached_system_prompt(csc_root: Path)  # Build system prompt from project context files with cache_control.
  def log_usage(turn: int, usage)  # Log token usage for a single turn, including cache stats.
  def run_anthropic_api(agent_name: str, model: str, user_prompt: str, csc_root: Path)  # Run workorder via Anthropic Messages API with tools + prompt caching.
  def run_claude_cli(agent_name: str, model: str, user_prompt: str, csc_root: Path)  # Invoke Claude Code CLI with system prompt (legacy fallback).
  def run_gemini(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Gemini CLI (installed locally, not via npx).
  def run_local(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke local model via ollama CLI.
  def main()

── ops/agents/opus/bin/run_agent.sh ──
# #!/bin/bash

── ops/agents/opus/cagent.yaml ──
# version: "2"

── ops/agents/opus/context/project-map.md ──
# # CSC Project Map - Quick Reference

── ops/agents/opus/context/test-guidelines.md ──
# # Test Writing Guidelines

── ops/agents/opus/orders.md-template ──
# You are an AI coding agent running from /opt/.

── ops/agents/opus/queue/out/agent_1773106893_csc-ftpd-wo-watcher.log ──
# Invoking: gemini -y -m gemini-2.5-pro -p " "

── ops/agents/opus/queue/out/agent_1773108135_csc-ftpd-wo-watcher.log ──
# Invoking: /home/davey/.local/bin/claude --dangerously-skip-permissions --model opus -p -

── ops/agents/opus/queue/out/agent_1773109225_IMPLEMENT_JULES_MONITORING_AND_APPROVAL_SERVICE.log ──
# Already up to date.

── ops/agents/opus/queue/out/agent_1773110037_IMPLEMENT_JULES_MONITORING_AND_APPROVAL_SERVICE.log ──
# Invoking: /home/davey/.local/bin/claude --dangerously-skip-permissions --model opus -p -

── ops/agents/opus/queue/out/agent_1773183743_EVOLUTION_MD_write_origin_story.log ──
# Invoking: /home/davey/.local/bin/claude --dangerously-skip-permissions --model opus -p - (cwd: /opt, repo: /opt/clones/opus/EVOLUTION_MD_write_origin_story-1773183743/repo)

── ops/agents/opus/queue/out/agent_1773185979_review_PR2_bridge_migration.log ──
# Invoking: /home/davey/.local/bin/claude --dangerously-skip-permissions --model opus -p - (cwd: /opt, repo: /opt/clones/opus/review_PR2_bridge_migration-1773185978/repo)

── ops/agents/opus/queue/out/agent_1773187533_platform_13-create_daily_motive_state_workorders.log ──
# Invoking: /home/davey/.local/bin/claude --dangerously-skip-permissions --model opus -p - (cwd: /opt, repo: /opt/clones/opus/platform_13-create_daily_motive_state_wo-1773187532/repo)

── ops/agents/opus/queue/out/agent_1773216377_PKI_CERTIFICATE_ENROLLMENT_SYSTEM.log ──
# Already up to date.

── ops/agents/opus/queue/out/orders-1772617733.md ──
# You are an AI coding agent. Your working directory is the CSC project root.

── ops/agents/opus/queue/out/orders-1772618228.md ──
# You are an AI coding agent. Your working directory is the CSC project root.

── ops/agents/opus/queue/out/orders-1772619439.md ──
# You are an AI coding agent. Your working directory is the CSC project root.

── ops/agents/opus/queue/out/orders-1772619801.md ──
# You are an AI coding agent. Your working directory is the CSC project root.

── ops/agents/opus/queue/out/orders-1773107328.md ──
# You are an AI coding agent. Your working directory is the CSC project root.

── ops/agents/opus/queue/out/orders-1773107474.md ──
# You are an AI coding agent. Your working directory is the CSC project root.

── ops/agents/opus/queue/out/orders-1773107620.md ──
# You are an AI coding agent. Your working directory is the CSC project root.

── ops/agents/opus/queue/out/orders-1773107767.md ──
# You are an AI coding agent. Your working directory is the CSC project root.

── ops/agents/opus/queue/out/orders-1773107914.md ──
# You are an AI coding agent. Your working directory is the CSC project root.

── ops/agents/opus/queue/out/orders-1773108061.md ──
# You are an AI coding agent. Your working directory is the CSC project root.

── ops/agents/opus/queue/out/orders-1773108211.md ──
# You are an AI coding agent. Your working directory is the CSC project root.

── ops/agents/opus/queue/out/orders-1773108370.md ──
# You are an AI coding agent. Your working directory is the CSC project root.

── ops/agents/opus/queue/out/orders-1773108533.md ──
# You are an AI coding agent. Your working directory is the CSC project root.

── ops/agents/opus/queue/out/orders-1773109964.md ──
# You are an AI coding agent. Your working directory is a clone of the CSC IRC code repo.

── ops/agents/opus/queue/out/orders-1773110111.md ──
# You are an AI coding agent. Your working directory is a clone of the CSC IRC code repo.

── ops/agents/opus/queue/out/orders-1773110259.md ──
# You are an AI coding agent. Your working directory is a clone of the CSC IRC code repo.

── ops/agents/opus/queue/out/orders-1773110416.md ──
# You are an AI coding agent. Your working directory is a clone of the CSC IRC code repo.

── ops/agents/opus/queue/out/orders-1773110576.md ──
# You are an AI coding agent.

── ops/agents/opus/queue/out/orders-1773110958.md ──
# You are an AI coding agent.

── ops/agents/opus/queue/out/orders-1773183955.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/opus/queue/out/orders-1773186203.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/opus/queue/out/orders-1773187891.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/opus/queue/out/orders-1773217374.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/opus/queue/out/orders-orphan-1773105521.md ──
# You are an AI coding agent. Your working directory is the CSC project root.

── ops/agents/opus/queue/out/orders-orphan-1773345383.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/opus/workorders/wip/opus_plan_queue_worker_python_implementation.md ──
# Starting task: reading reference materials

── ops/agents/qwen/bin/run_agent.bat ──
# @echo off

── ops/agents/qwen/bin/run_agent.py ──
# Universal agent runner — detects agent type from directory name and
  CLAUDE_AGENTS = {'haiku': 'claude-haiku-4-5-20251001', 'sonnet': 'claude-sonnet-4-5-20250929'...
  GEMINI_AGENTS = {'gemini-3-pro': 'gemini-2.5-pro', 'gemini-3-flash': 'gemini-2.5-flash', 'gem...
  LOCAL_AGENTS = {'ollama-codellama': 'codellama', 'ollama-deepseek': 'deepseek-coder', 'ollam...
  def detect_agent_name(workorder_path: Path) -> str  # Derive agent name from directory structure or environment.
  def find_csc_root(workorder_path: Path) -> Path  # Walk up from workorder to find project root (has CLAUDE.md).
  def _make_env(**extras)  # Create a clean subprocess environment with UTF-8 encoding.
  def run_claude(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Claude Code CLI.
  def run_gemini(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Gemini CLI via npx.
  def run_local(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke local model via ollama CLI.
  def main()

── ops/agents/qwen/bin/run_agent.sh ──
# #!/bin/bash

── ops/agents/qwen/cagent.yaml ──
# version: "2"

── ops/agents/qwen/context/project-map.md ──
# # CSC Project Map - Quick Reference

── ops/agents/qwen/context/test-guidelines.md ──
# # Test Writing Guidelines

── ops/agents/sonnet/bin/run_agent.bat ──
# @echo off

── ops/agents/sonnet/bin/run_agent.py ──
# Universal agent runner — detects agent type from directory name and
  ANTHROPIC_AGENTS = {'haiku': 'claude-haiku-4-5-20251001', 'sonnet': 'claude-sonnet-4-5-20250929'...
  CLAUDE_AGENTS = ANTHROPIC_AGENTS
  GEMINI_AGENTS = {'gemini-3-pro': 'gemini-2.5-pro', 'gemini-3-flash': 'gemini-2.5-flash', 'gem...
  LOCAL_AGENTS = {'ollama-codellama': 'codellama', 'ollama-deepseek': 'deepseek-coder', 'ollam...
  MAX_TURNS = 100
  OUTPUT_CAP = 50000
  MAX_TOKENS_BY_MODEL = {'claude-opus-4-6': 32768, 'claude-sonnet-4-5-20250929': 32768, 'claude-haiku...
  DEFAULT_MAX_TOKENS = 16384
  SKIP_DIRS = {'.git', 'node_modules', '.trash', '__pycache__', '.venv', 'venv', '.tox'}
  CLAUDE_SYSTEM_PROMPT = 'You are an AI coding agent working on the CSC project. Your working director...
  def detect_agent_name(workorder_path: Path) -> str  # Derive agent name from directory structure or environment.
  def find_csc_root(workorder_path: Path) -> Path  # Walk up from workorder to find project root (has CLAUDE.md).
  def _make_env(**extras)  # Create a clean subprocess environment with UTF-8 encoding.
  def build_tools()  # Define the 6 tools available to Anthropic agents, matching Claude Code.
  def _safe_path(csc_root: Path, relative: str) -> Path  # Resolve a relative path within csc_root, preventing directory traversal.
  def execute_tool(name: str, tool_input: dict, csc_root: Path) -> str  # Execute a tool call locally, return result string.
  def build_cached_system_prompt(csc_root: Path)  # Build system prompt from project context files with cache_control.
  def log_usage(turn: int, usage)  # Log token usage for a single turn, including cache stats.
  def run_anthropic_api(agent_name: str, model: str, user_prompt: str, csc_root: Path)  # Run workorder via Anthropic Messages API with tools + prompt caching.
  def run_claude_cli(agent_name: str, model: str, user_prompt: str, csc_root: Path)  # Invoke Claude Code CLI with system prompt (legacy fallback).
  def run_gemini(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Gemini CLI (installed locally, not via npx).
  def run_local(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke local model via ollama CLI.
  def main()

── ops/agents/sonnet/bin/run_agent.sh ──
# #!/bin/bash

── ops/agents/sonnet/orders.md-template ──
# You are an AI coding agent running from /opt/.

── ops/agents/sonnet/queue/out/agent_1773105745_PROMPT_review_pr_daveylongshaft_irc_1_20260309-201234.log ──
# Updating 083aef9..b890419

── ops/agents/sonnet/queue/out/agent_1773106893_csc-ftpd-wo-watcher.log ──
# Invoking: gemini -y -m gemini-2.5-pro -p " "

── ops/agents/sonnet/queue/out/agent_1773108135_csc-ftpd-wo-watcher.log ──
# Invoking: /home/davey/.local/bin/claude --dangerously-skip-permissions --model opus -p -

── ops/agents/sonnet/queue/out/agent_1773109225_IMPLEMENT_JULES_MONITORING_AND_APPROVAL_SERVICE.log ──
# Already up to date.

── ops/agents/sonnet/queue/out/agent_1773111393_PLAN_REVIEW_AGENT_SETUP.log ──
# Already up to date.

── ops/agents/sonnet/queue/out/agent_1773112400_REBUILD_AGENT_AND_WORKORDERS_SERVICES.log ──
# Invoking: /home/davey/.local/bin/claude --dangerously-skip-permissions --model sonnet -p - (cwd: /opt, repo: /opt/csc/tmp/sonnet/REBUILD_AGENT_AND_WORKORDERS_SERVICES-1773112399/repo)

── ops/agents/sonnet/queue/out/agent_1773113892_PROMPT_review_pr_daveylongshaft_irc_1_20260309-175457.log ──
# Already up to date.

── ops/agents/sonnet/queue/out/agent_1773186948_PROMPT_review_pr_daveylongshaft_irc_2_20260310-185059.log ──
# Already up to date.

── ops/agents/sonnet/queue/out/agent_1773192698_PROMPT_review_pr_daveylongshaft_irc_3_20260310-202906.log ──
# Already up to date.

── ops/agents/sonnet/queue/out/agent_1773206689_oper-hierarchy-20260311.log ──
# Invoking: /home/davey/.local/bin/claude --dangerously-skip-permissions --model sonnet -p - (cwd: /opt, repo: /opt/clones/sonnet/oper-hierarchy-20260311-1773206688/repo)

── ops/agents/sonnet/queue/out/agent_1773217742_PKI_CERTIFICATE_ENROLLMENT_SYSTEM.log ──
# Invoking: /home/davey/.local/bin/claude --dangerously-skip-permissions --model sonnet -p - (cwd: /opt, repo: /opt/clones/sonnet/PKI_CERTIFICATE_ENROLLMENT_SYSTEM-1773217741/repo)

── ops/agents/sonnet/queue/out/agent_1773228754_PROMPT_setup_github_webhook.log ──
# Invoking: /home/davey/.local/bin/claude --dangerously-skip-permissions --model sonnet -p - (cwd: /opt, repo: /opt/clones/sonnet/PROMPT_setup_github_webhook-1773228753/repo)

── ops/agents/sonnet/queue/out/agent_1773229264_windows-service-update.log ──
# Already up to date.

── ops/agents/sonnet/queue/out/orders-1773106277.md ──
# You are an AI coding agent. Your working directory is the CSC project root.

── ops/agents/sonnet/queue/out/orders-1773107182.md ──
# You are an AI coding agent. Your working directory is the CSC project root.

── ops/agents/sonnet/queue/out/orders-1773109808.md ──
# You are an AI coding agent. Your working directory is a clone of the CSC IRC code repo.

── ops/agents/sonnet/queue/out/orders-1773112036.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/sonnet/queue/out/orders-1773112682.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/sonnet/queue/out/orders-1773188182.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/sonnet/queue/out/orders-1773192140.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/sonnet/queue/out/orders-1773193166.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/sonnet/queue/out/orders-1773207310.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/sonnet/queue/out/orders-1773218203.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/sonnet/queue/out/orders-1773223845.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/sonnet/queue/out/orders-1773229093.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/sonnet/queue/out/orders-1773229493.md ──
# You are an AI coding agent running from /opt/.

── ops/agents/templates/README.md ──
# # Agent Task Templates

── ops/agents/templates/default.md ──
# ---

── ops/agents/templates/generate_orders_md.bat ──
# @echo off

── ops/agents/templates/generate_orders_md.sh ──
# #!/bin/bash

── ops/agents/templates/orders.md-template ──
# You are an AI coding agent running from /opt/.

── ops/agents/templates/run_agent.bat ──
# @echo off

── ops/agents/templates/run_agent.py ──
# Universal agent runner — detects agent type from directory name and
  ANTHROPIC_AGENTS = {'haiku': 'claude-haiku-4-5-20251001', 'sonnet': 'claude-sonnet-4-5-20250929'...
  CLAUDE_AGENTS = ANTHROPIC_AGENTS
  GEMINI_AGENTS = {'gemini-3-pro': 'gemini-2.5-pro', 'gemini-3-flash': 'gemini-2.5-flash', 'gem...
  LOCAL_AGENTS = {'ollama-codellama': 'codellama', 'ollama-deepseek': 'deepseek-coder', 'ollam...
  MAX_TURNS = 100
  OUTPUT_CAP = 50000
  MAX_TOKENS_BY_MODEL = {'claude-opus-4-6': 32768, 'claude-sonnet-4-5-20250929': 32768, 'claude-haiku...
  DEFAULT_MAX_TOKENS = 16384
  SKIP_DIRS = {'.git', 'node_modules', '.trash', '__pycache__', '.venv', 'venv', '.tox'}
  CLAUDE_SYSTEM_PROMPT = 'You are an AI coding agent working on the CSC project. Your working director...
  def detect_agent_name(workorder_path: Path) -> str  # Derive agent name from directory structure or environment.
  def find_csc_root(workorder_path: Path) -> Path  # Walk up from workorder to find project root (has CLAUDE.md).
  def _make_env(**extras)  # Create a clean subprocess environment with UTF-8 encoding.
  def build_tools()  # Define the 6 tools available to Anthropic agents, matching Claude Code.
  def _safe_path(csc_root: Path, relative: str) -> Path  # Resolve a relative path within csc_root, preventing directory traversal.
  def execute_tool(name: str, tool_input: dict, csc_root: Path) -> str  # Execute a tool call locally, return result string.
  def build_cached_system_prompt(csc_root: Path)  # Build system prompt from project context files with cache_control.
  def log_usage(turn: int, usage)  # Log token usage for a single turn, including cache stats.
  def run_anthropic_api(agent_name: str, model: str, user_prompt: str, csc_root: Path)  # Run workorder via Anthropic Messages API with tools + prompt caching.
  def run_claude_cli(agent_name: str, model: str, user_prompt: str, csc_root: Path)  # Invoke Claude Code CLI with system prompt (legacy fallback).
  def run_gemini(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Gemini CLI (installed locally, not via npx).
  def run_local(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke local model via ollama CLI.
  def main()

── ops/agents/templates/run_agent.sh ──
# #!/bin/bash

── ops/agents/test-agent/bin/run_agent.bat ──
# @echo off

── ops/agents/test-agent/bin/run_agent.py ──
# Universal agent runner — detects agent type from directory name and
  CLAUDE_AGENTS = {'haiku': 'claude-haiku-4-5-20251001', 'sonnet': 'claude-sonnet-4-5-20250929'...
  GEMINI_AGENTS = {'gemini-3-pro': 'gemini-2.5-pro', 'gemini-3-flash': 'gemini-2.5-flash', 'gem...
  LOCAL_AGENTS = {'ollama-codellama': 'codellama', 'ollama-deepseek': 'deepseek-coder', 'ollam...
  def detect_agent_name(workorder_path: Path) -> str  # Derive agent name from directory structure or environment.
  def find_csc_root(workorder_path: Path) -> Path  # Walk up from workorder to find project root (has CLAUDE.md).
  def _make_env(**extras)  # Create a clean subprocess environment with UTF-8 encoding.
  def run_claude(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Claude Code CLI.
  def run_gemini(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke Gemini CLI via npx.
  def run_local(agent_name: str, model: str, workorder_content: str, csc_root: Path)  # Invoke local model via ollama CLI.
  def main()

── ops/agents/test-agent/bin/run_agent.sh ──
# #!/bin/bash
